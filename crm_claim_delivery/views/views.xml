<?xml version="1.0"?>
<openerp>
    <data>

        <record model="ir.ui.view" id="crm_claim_inherit">
            <field name="name">Claim Form delivery extension</field>
            <field name="model">crm.claim</field>
            <field name="inherit_id" ref="crm_claim.crm_case_claims_form_view"/>
            <field name="arch" type="xml">
                <field name="date" position="after">
                    <field name="delivery_id"  />
                    <separator string="Products in claim" 
                        attrs="{'invisible':[('delivery_id','=', False)]}"/>
                    <group col="2" colspan="2">
                        <group col="1" colspan="1">
                            <field name="product_selection_ids" widget="many2many_list" 
                                attrs="{'invisible':[('delivery_id','=', False)]}"/>
                        </group>
                        <group col="1" colspan="1">
                            <field name="product_selected_ids" widget="many2many_list" 
                                attrs="{'invisible':[('delivery_id','=', False)]}"  />
                        </group>
                    </group>
                </field>
            </field>

        </record>


    </data>
</openerp>
